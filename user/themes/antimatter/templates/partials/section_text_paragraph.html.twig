{% if paragraph is iterable %}
    {{ paragraph|var_dump }}
{% else %}
    {% if paragraph == '~list~' %}
    	{% include 'partials/list.html.twig' with {'list': section.list} %}
    {% else %}
        {{ paragraph|markdown }}
    {% endif %}
{% endif %}
